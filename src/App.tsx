import { Button } from 'antd';
import React, { FC } from 'react';
import './App.css';

const App: FC = () => (
    <div className="App">
        <Button type="primary">Button</Button>
    </div>
);

export default App;